const attributes = {
    checkboxControl: {
        type: 'boolean',
        default: true,
    },
    dateTimeControl: {
        type: 'string',
    },
    textControl: {
        type: 'string',
        default: 'Text for value one', 
    },
    toggleControl: {
        type: 'boolean',
    },
    selectControl: {
        type: 'string',
    },
};

export default attributes;
